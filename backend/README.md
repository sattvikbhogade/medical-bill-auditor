# Backend

This package contains the FastAPI backend scaffold for the Medical Bill Auditor platform.

The application is organized into domain-oriented modules so new features can be added without expanding a single monolithic entry point.
